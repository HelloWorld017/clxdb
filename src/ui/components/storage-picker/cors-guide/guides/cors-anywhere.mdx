import { LocationOrigin } from '../location-origin.tsx';
import { ThirdPartyCodeWarning, UntestedWarning } from '../warnings.tsx';

<ThirdPartyCodeWarning />
<UntestedWarning />

## Description
CORS Anywhere is a proxy, which allows applications to connect arbitrary servers.

## How-To

1. **Deploy following docker image**  
   [testcab/cors-anywhere](https://hub.docker.com/r/testcab/cors-anywhere)

2. **Set `CORSANYWHERE_WHITELIST` environment as following:**<br /><LocationOrigin />
